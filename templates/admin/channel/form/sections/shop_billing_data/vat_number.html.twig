{% set form = hookable_metadata.context.form %}

{% if form.vatNumber is defined %}
    <div class="col-12 col-md-6">
        {{ form_row(form.vatNumber, sylius_test_form_attribute('vat-number')) }}
    </div>
{% endif %}
